# Conteúdo do arquivo .env
# --- Configurações básicas ---
AIRFLOW_IMAGE_NAME=apache/airflow:latest
AIRFLOW_UID=50000

# --- Caminhos para montagens (Se você mudou no docker-compose, ajuste aqui ou remova) ---
# Nota: Como corrigimos o YAML para usar ./dags em vez de ${AIRFLOW_DAGS}, estas linhas são menos críticas
# para o sucesso da montagem, mas são boas práticas para o UID e o nome da imagem.
AIRFLOW_DAGS=./dags
AIRFLOW_LOGS=./logs
AIRFLOW_PLUGINS=./plugins
AIRFLOW_SECRETS=./secrets